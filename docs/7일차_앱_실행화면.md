# 7일차 - 앱 실행화면

## 환자 정보 수정 (`PATCH /api/v1/patients/{patient_id}`)

`/patients/{id}` 환자 상세 페이지에서 "정보 수정" 버튼을 눌러 이름·전화번호를 수정하는 화면입니다.

<img src="assets/환자 정보 수정.jpg" width="600" alt="환자 정보 수정">

## 진료 기록 등록 (`POST /api/v1/patients/{patient_id}/medical-records`)

`/patients/{id}/medical-records/create`에서 차트 번호·증상·X-Ray 이미지를 입력해 진료 기록을 등록하는 화면입니다.

<img src="assets/진료 기록 등록.jpg" width="600" alt="진료 기록 등록">

## 진료 기록 등록 완료 (`GET /api/v1/patients/{patient_id}/medical-records`)

등록 완료 후 `/patients/{id}`로 돌아와 진료 기록 목록에 새 기록이 반영된 것을 확인한 화면입니다.

<img src="assets/진료 기록 등록 완료.jpg" width="600" alt="진료 기록 등록 완료">
